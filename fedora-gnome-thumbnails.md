# Thumbnails not displaying on Fedora with GNOME

```shell
sudo dnf install glyph-thumbnailer # For Images
sudo dnf install ffmpegthumbnailer # For videos
sudo install poppler-utils envice-thumbnailer # For PDFs
```
